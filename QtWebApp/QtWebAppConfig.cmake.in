@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Qt6 REQUIRED COMPONENTS Core Network)
if(Qt6_VERSION VERSION_GREATER "6.0")
    find_dependency(Qt6 COMPONENTS Core5Compat)
endif()

# Include the targets file
include("${CMAKE_CURRENT_LIST_DIR}/QtWebAppTargets.cmake")

check_required_components(QtWebApp)